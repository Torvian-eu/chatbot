{
  /*
    DEVELOPMENT OVERRIDE: config.json
    This file is loaded in Development Mode (when running from IDE)
    and contains settings specific to the local development environment.
  */
  "network": {
    "serverUrl": "http://localhost:8080"
  },
  "storage": {
    "dataDir": "data-dev",
    "tokenStorageDir": "tokens-dev",
    "certificateStorageDir": "certs-dev"
  }
}