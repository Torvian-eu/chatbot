{
  /*
    Production setup.json
    - On first deployment, set "required": true so the worker will run setup and generate secrets.json.
    - After setup completes the setup process typically writes setup.json with required = false.
  */
  "setup": {
    "required": true
  }
}
